using UnityEngine;

public class WeatherCustomParamsModule: MonoBehaviour
{
    public bool IsModuleValid { get; private set; }

    public void SetParameters()
    {
        //_skyVolume.TryGetComponent<PhysicallyBasedSky>(out PhysicallyBasedSky PBS);

        //PBS.aerosolDensity;
    }
}